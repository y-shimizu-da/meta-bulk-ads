# システムユーザートークンで使う手順（推奨・社内運用）

FBログイン不要・テスター招待不要・App Review不要。ビジネスマネージャで一度作れば、チームでツールの「手動でトークンを入力」に貼って使えます。トークンは無期限にできます。

ツールURL: **https://www.digitalathlete.jp/lp/meta-bulk-ads/**
対象アプリID: **1699295671486963**

## 前提
- あなたが **ビジネスマネージャの管理者** であること。
- 入稿したい **広告アカウント・Facebookページ** が、そのビジネスマネージャに入っている（または共有されている）こと。

---

## STEP 1　（未対応なら）アプリをビジネスに追加
1. **https://business.facebook.com/settings** を開く（左上で対象ビジネスを選択）
2. 左メニュー **「アカウント」→「アプリ」→「追加」**
3. アプリID **1699295671486963** を追加
   ※すでに追加済みならスキップ。

## STEP 2　システムユーザーを作成
1. ビジネス設定の左メニュー **「ユーザー」→「システムユーザー」→「追加」**
2. 名前: 例 `meta-bulk-ads`
3. 役割: **管理者システムユーザー**（Admin）
4. 作成

## STEP 3　システムユーザーに資産を割り当て
作成したシステムユーザーを選択 →「**資産を割り当て**」で以下を付与:
- **広告アカウント**（入稿先すべて）→ 権限「**広告を管理**」（フルコントロール推奨）
- **Facebookページ**（クリエイティブに使うページ）→ 「**フルコントロール**」
- （Instagram配信を使う場合）連携している **Instagramアカウント** も

> 後から新しい広告アカウント／ページを使う時は、その都度ここで割り当ててください。

## STEP 4　トークンを生成
1. システムユーザーの画面で「**新しいトークンを生成**」
2. アプリ: **1699295671486963** を選択
3. 有効期限: **無期限（Never）** を選択可
4. アクセス許可（スコープ）に次をチェック:
   - `ads_management`
   - `pages_show_list`
   - `pages_read_engagement`
   - `business_management`
5. 生成 → **トークンが表示される（1回だけ）**。安全な場所に保管。

## STEP 5　ツールで使う
1. **https://www.digitalathlete.jp/lp/meta-bulk-ads/** を開く
2. 「**手動でトークンを入力**」の「表示」を開く
3. 入力:
   - **アクセストークン**: STEP4で生成したトークン
   - **広告アカウントID**: `act_数字`（数字だけでも自動で `act_` が付きます）
   - **ページID**: 使うFacebookページのID
4. セットアップ → 通常どおり入稿（フォーム／CSV／スプレッドシート）

### ID の調べ方
- **広告アカウントID**: 広告マネージャ、またはビジネス設定 →「広告アカウント」に表示（`act_` の後ろの数字）
- **ページID**: Facebookページの「設定 → ページ情報」、またはビジネス設定 →「ページ」

---

## セキュリティ・運用の注意
- トークンは**広告を操作できる強力な鍵**です。**社外や公開の場に貼らない**でください。
  - ツールはトークンを**サーバーに送らず**、ブラウザからMeta APIへ直接使うだけ。**保存もしません**（ページ再読み込みで消えます）。
- 漏れた／担当者交代の時は、システムユーザーで**トークンを再生成**（＝旧トークン無効化）または該当システムユーザーを削除。
- 個人ごとに操作を分けたい場合は、システムユーザーを複数作る運用も可能。
- **標準アクセス（App Review未実施）でも、自社ビジネスの資産を操作する分には問題なく動きます。**

## BANリスクの観点でも有利
操作が「個人アカウント」ではなく「ビジネスの資産（システムユーザー）」に紐づくため、個人アカウントを矢面に立たせません。あわせて、全エンティティ PAUSED 作成→内容確認後に有効化、広告・LPのポリシー順守、を守れば安全側の運用になります。
